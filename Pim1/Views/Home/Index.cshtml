@model HomeModel
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Bem Vindo(a) @Model.Nome !</h1>
    <p>Sistema Fazenda Urbana - PIM &copy;</a>.</p>
    <span>Seu Email &eacute; @Model.Email </span>
</div>

